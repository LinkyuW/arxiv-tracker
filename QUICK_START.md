# 快速开始指南

## 5分钟快速启动

### 1. 准备工作
```bash
# 克隆项目
git clone <repo-url>
cd arxiv-tracker

# 安装后端依赖
cd backend
pip install -r requirements.txt
cd ..
```

### 2. 配置API密钥
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件，填入你的 Google Gemini API密钥
# 从这里获取: https://aistudio.google.com/app/apikeys
nano .env
```

### 3. 启动后端
```bash
cd backend
python app.py
```

看到以下输出说明成功:
```
 * Running on http://127.0.0.1:5000
```

### 4. 启动前端
在新的终端窗口:
```bash
cd frontend
python -m http.server 8000
```

### 5. 打开浏览器
访问: **http://localhost:8000**

## 首次使用

1. **搜索论文**
   - 在搜索框输入: "machine learning"
   - 勾选 "使用AI总结"
   - 点击搜索

2. **查看结果**
   - 等待搜索完成
   - 点击论文卡片查看详情
   - 查看AI生成的中文总结

3. **缓存管理**
   - 底部点击 "缓存统计" 查看缓存信息
   - 第二次搜索同样的关键词会更快 (来自缓存)

## 常见问题

### 后端无法启动?
```bash
# 检查Python版本
python --version  # 需要 3.8+

# 检查依赖安装
pip list | grep -i flask

# 查看是否有端口冲突
# 编辑 backend/app.py 最后一行改为其他端口
# app.run(host='0.0.0.0', port=5001)  # 改为 5001
```

### 无法获取API密钥?
1. 访问 https://aistudio.google.com/app/apikeys
2. 使用Google账号登录 (需要国内能访问Google)
3. 点击 "Create API Key"
4. 复制到 .env 文件

### 搜索很慢或无结果?
- 检查网络连接
- 尝试更简单的关键词 (如: "AI" 而不是 "深度神经网络优化")
- 禁用AI总结以加快速度 (取消勾选)

## 项目结构

```
backend/           # Flask后端服务
├── app.py        # 主应用和API路由
├── config.py     # 配置管理
├── requirements.txt
└── services/     # 核心服务
    ├── arxiv_service.py   # arXiv论文搜索
    ├── ai_service.py      # AI总结
    └── cache_service.py   # 缓存管理

frontend/         # 前端界面
├── index.html    # 主页面
├── style.css     # 样式表
└── src/main.js   # 交互逻辑

database/         # 数据库模型 (可选)
└── models.py
```

## 核心功能代码位置

| 功能 | 文件 | 行数 |
|------|------|------|
| arXiv搜索 | backend/services/arxiv_service.py | 40-80 |
| AI总结 | backend/services/ai_service.py | 30-50 |
| 缓存系统 | backend/services/cache_service.py | 全文 |
| API路由 | backend/app.py | 65-110 |
| 前端搜索 | frontend/src/main.js | 55-100 |
| 结果展示 | frontend/src/main.js | 180-220 |

## 下一步

- [ ] 读完 README.md 了解完整功能
- [ ] 修改搜索时间范围 (backend/config.py:19)
- [ ] 自定义UI样式 (frontend/style.css)
- [ ] 部署到GitHub Pages 或服务器
- [ ] 添加更多搜索过滤选项
- [ ] 集成用户账户系统

## 获得帮助

- 查看 README.md 获取详细文档
- 检查 .env.example 了解所有配置选项
- 查看代码中的注释 (所有关键函数都有)
- GitHub Issues 报告问题

---

**开心使用！** 如有问题欢迎反馈 🚀
